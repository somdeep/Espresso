class Test {

int main(int a) {
    int x;
    int y;
	for(x =0; x <10;x = x +1) {
	    x = x +2;
	    y = 10;
	    while(y < 20){
		    if((x==2)&&(y == 15))
			   print_int(x+y);
	    	y = y + 1 ;
	    }
	} 
    return 0;
   }
}