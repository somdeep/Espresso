class Test {

int main(int a) {
    int x;
    x = 0;
	while(x < 20) {
	    x = x +2;
	    int y;
	    y =2;
	    for(y =0; y <10;y = y +1){
	    	if((x==2) && (y ==0))
			    print_int(x+y);
			y = y + 1 ;
			
	    }
	} 
    return 0;
   }
}